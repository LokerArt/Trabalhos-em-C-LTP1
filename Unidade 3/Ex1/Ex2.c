#include <stdio.h>

int main() {
    float base = 0, num;
    printf("Digite o Numero:");
    scanf("%f" , &num);
       if (base <= num) {
        printf("Positivo");
       }
       else if (base > num) {
        printf("Negativo");
       }
    }

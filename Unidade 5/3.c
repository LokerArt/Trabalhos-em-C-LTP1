#include <stdio.h>
#include <stdlib.h>

void fazoqquiser(int Quant) {
    int i;
    for (i = 1; i <= Quant; i++){
        printf("%d", simbolo);
    }
}

int main() {
    int cont, simbolo;
    printf("Qual simbolo deseja que retorne meu bom?");
    scanf("%d", &simbolo);
    printf("Quantos simbolos deseja que retorne meu bom?")
    scanf("%d", &cont);
    fazoqquiser(cont, simbolo);

    return 0;
}